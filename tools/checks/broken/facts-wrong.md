# A document that is wrong on purpose

Nothing here is true. It exists so `check-facts.sh` can be proved to REFUSE as well as
to pass, which is the rule this repo keeps for every check: a check that only ever says
yes has not been shown to work.

Every sentence below restates a fact the code holds and gets it wrong, and each one is
wrong in the way a real document went wrong.

`PenetrationSettings.DefaultSolidCategories` holds three categories. That is the exact
error `tools/probes/fixture.md` carried on 2026-09-21, written with confidence, and the
person reading it would have built the wrong fixture off it.

Structural Foundations is not on that list. That is the second half of the same error,
and it is the half that would have done the damage.

Pipes, ducts, cable trays and their fittings OVER 250 mm go in the viewpoints and
smaller ones do not.

It says how many tick boxes are visible without opening anything, which must be 7.

One row per event, twelve columns, tab separated, with a header.
